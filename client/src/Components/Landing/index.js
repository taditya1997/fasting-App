import React from 'react';
import FastingMeter from '../FastingMeter';

const Landing = (props) => {
    return (
        
        <div className="Fasting-Tracker">
        <FastingMeter/>

        </div>
    );
};

export default Landing;